package FoodSense.Sorter;

import java.util.LinkedList;

/**
 * @author Benjamin Grimes
 */
public class AprioriCalculatorService {

    /***************************************************************************************
     * runs Apriori's Algorithm on the incoming purchases and returns the results.
     *
     * @param  purchases
     * All items that were bought. Items bought multiple times should be listed multiple times
     *
     * @return
     * A linkedList of MatrixNodes for easy addition to matrix
     ******************************************************************************************/
    protected static LinkedList<ReorganizeModel.MatrixNode> AprioriCalc(LinkedList<Integer> purchases){
        //TODO
        return new LinkedList<ReorganizeModel.MatrixNode>();
    }
}
